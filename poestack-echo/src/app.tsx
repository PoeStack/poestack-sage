import * as ReactDOM from 'react-dom';

function render() {
  ReactDOM.render(<h2 className="bg-gray-300 text-center text-blue-800">Hello fromasdasdas React! {"Zach"}</h2>, document.body);
}

render();